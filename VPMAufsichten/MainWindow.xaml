<Window x:Class="VPMAufsichten.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VPMAufsichten"
        mc:Ignorable="d"
        Title="VPM Aufsichtsplan" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0" Grid.RowSpan="1" Grid.ColumnSpan="2" >
            <Button Content="Öffnen" Click="Button_Click"/>
            <Separator/>

            <ComboBox x:Name="uiPause" SelectionChanged="ComboBox_SelectionChanged" Width="130">
                <ComboBoxItem Content="Frühaufsicht" Tag="1"/>
                <ComboBoxItem Content="1. Pause" Tag="2"/>
                <ComboBoxItem Content="2. Pause" Tag="3"/>
                <ComboBoxItem Content="Mittagsaufsicht" Tag="4"/>
            </ComboBox>
            <CheckBox x:Name="uiHideEmpty" Content="leere Zeilen ausblenden" IsChecked="False" Checked="uiHideEmpty_Checked" Unchecked="uiHideEmpty_Checked"/>
        </ToolBar>
        <DataGrid x:Name="uiGrid" Grid.Column="0" Grid.Row="1" d:ItemsSource="{d:SampleData ItemCount=5}"
          AutoGenerateColumns="False" 
          CanUserAddRows="False" 
          SelectionUnit="Cell"
          GridLinesVisibility="All"
          FontSize="14"
          AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Aufsichtsort" 
                            Binding="{Binding Ort}" 
                            IsReadOnly="True" 
                            Width="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Right"/>
                            <Setter Property="Background" Value="#EEEEEE"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="5,2"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Montag" Binding="{Binding Montag}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Dienstag" Binding="{Binding Dienstag}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Mittwoch" Binding="{Binding Mittwoch}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Donnerstag" Binding="{Binding Donnerstag}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Freitag" Binding="{Binding Freitag}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
